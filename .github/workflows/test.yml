name: SEPT Pipeline

on:
  push:
    branches:
      - main
      - temp
  pull_request:
    branches:
      - main
jobs:
  test-run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: test echo
      - run: echo "Hello, World!"
