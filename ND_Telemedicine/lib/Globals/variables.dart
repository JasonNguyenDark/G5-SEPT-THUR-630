//this currently used for local testing change this once we host it on remote
// for local to work right click main.dart then click 'modify run configuration'
// in additional run args add this: --web-port=8000
const baseUrl = "http://localhost:8080/#";
const Map<String, String> headers = {"Content-Type": "application/json"};